#mongodb
spring:
  data:
    mongodb:
      host: 127.0.0.1
      port: 27017
      database: mydatabase

#server
server:
  servlet:
    context-path: /api

#logging
logging:
  level:
    org.springframework.data: debug
